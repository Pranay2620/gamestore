import styled from "styled-components";

export const GameContainerStyle = styled.section`
    width: 70%;
    display: grid;
    grid-template-columns: repeat(auto-fit, 32%);
    justify-content: space-between;
    margin: 5rem auto;
    row-gap: 2rem;
    overflow-y: hidden;
`

